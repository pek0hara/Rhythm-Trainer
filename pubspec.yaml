name: rhythm_trainer
description: Rhythm training single-screen BPM analyzer.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  path_provider: ^2.1.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.0
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
